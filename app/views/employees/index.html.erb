<h1>Employees</h1>

<ul> <% @employees.each do |e| %>
    <li><%= link_to e.first_name + " " + e.last_name, employee_path(e) %> (<%= e.title %>) </li>
<% end %>
</ul>

<%= button_to 'Hire new employee', new_employee_path, method: :get %>


